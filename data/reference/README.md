# Reference Data

Optional lookup files used to enrich TLC LocationIDs.

Supported optional file:

- `taxi_zone_lookup.csv`: NYC TLC taxi zone lookup with `LocationID`, `Borough`, `Zone`, and `service_zone` columns.
